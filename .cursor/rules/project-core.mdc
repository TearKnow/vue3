---
description: 项目通用规范
alwaysApply: true
---

# 项目通用规范

- 优先复用现有模式、命名和目录结构，不随意引入新风格。
- 样式相关改动遵循变量优先原则，不新增硬编码颜色。
- 项目内使用 `NuxtLink` 时默认添加 `no-prefetch`，避免首屏批量链接预取导致线上样式抖动；只有明确需要预加载优化时才例外。
- 对已有代码做最小必要改动，避免与当前需求无关的大范围重构。
- 完成改动后，运行对应的 lint 或最小验证命令再给出结论。
