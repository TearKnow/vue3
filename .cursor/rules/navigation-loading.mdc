---
description: 站内链接导航须提供全屏 loading 反馈
alwaysApply: true
---

# 站内导航 Loading

站内路由切换（`NuxtLink` / `navigateTo` / `router.push`）须有全屏 loading，让用户确认点击已生效。

## 实现

- 统一使用 `composables/useNavigationLoading.ts`，在 `app.vue` 调用一次 `useNavigationLoading()`。
- **不要**为单个页面/板块再写一套独立 overlay；扩展现有 composable。
- path 未变（仅 hash / 同 path 的 query 微调）可不展示；首屏初始化导航不展示。
- 外链、`target="_blank"` 不走路由，无需 loading。

## 关闭时机

- 默认：Nuxt `page:loading:end` 后再等一帧绘制再关（普通页）。
- **blog / wiki** 等正文异步渲染页：不要依赖路由结束自动关；在 `pending === false` 和/或 `onMounted` 时调用 `removeNavigationLoadingOverlay()`。过早在 `afterEach` 关闭会出现「转圈没了、内容才变」的错位。

## 示例

```ts
// ❌ BAD：目标还是旧正文时就 remove；或 wiki/blog 依赖 afterEach 关遮罩
// ✅ GOOD：useNavigationLoading + 内容页手动 removeNavigationLoadingOverlay()
```
